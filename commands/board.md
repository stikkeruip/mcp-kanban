---
description: Browse the kanban board interactively (columns, tasks, move/archive/resume)
---

Call the tasks MCP `browse_board` tool with no arguments. It opens an interactive, client-rendered browsing session — do not narrate while it runs.

When it returns, report its `actions` list as a short bullet recap (or the single line it contains if nothing changed). Nothing else.
