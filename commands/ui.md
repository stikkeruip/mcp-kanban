---
description: Open the drag-and-drop kanban board in the browser
---

Call the tasks MCP `open_board` tool with no arguments. It starts the local web server if needed and opens the board in the default browser.

Report just the URL it returns (and note if it had to start the server). Nothing else.
